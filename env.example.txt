# Browser build: Supabase project information is currently generated in
# utils/supabase/info.ts by the Figma/Supabase integration. Do not put LiveKit
# secrets in VITE_* variables.

# Supabase Edge Function secrets required by the unified voice transport:
#   supabase secrets set LIVEKIT_URL=wss://your-project.livekit.cloud
#   supabase secrets set LIVEKIT_API_KEY=...
#   supabase secrets set LIVEKIT_API_SECRET=...
#
# Supabase provides SUPABASE_URL, SUPABASE_ANON_KEY and
# SUPABASE_SERVICE_ROLE_KEY to Edge Functions automatically.
#
# For self-hosted LiveKit, LIVEKIT_URL should be the public wss:// endpoint
# reachable by browsers. Keep the API secret server-side only.
